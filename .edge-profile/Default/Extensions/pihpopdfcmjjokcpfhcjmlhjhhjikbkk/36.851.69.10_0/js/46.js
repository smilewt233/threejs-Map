let assert=chai.assert;let map=window.L.map(document.createElement("div"),{maxZoom:20,inertia:false,keyboard:false});map.setView([0,0],0);let makeOne=()=>{let e=leafletLayer({scene:sampleScene,disableRenderLoop:true});return e};describe("Leaflet plugin",()=>{describe(".constructor()",()=>{let e;beforeEach(()=>{e=makeOne()});afterEach(()=>{e.scene.destroy()});it("returns a new instance",()=>{assert.instanceOf(e,LeafletLayer)});it("wires up the scene",()=>{assert.instanceOf(e.scene,Scene)})});describe(".addTo(map)",()=>{let s;beforeEach(function(e){s=makeOne();sinon.spy(s,"getContainer");sinon.spy(s.scene,"load");s.on("init",()=>{e()});s.addTo(map)});afterEach(()=>{s.remove();s.getContainer.restore()});it("calls the layer's .getContainer() method",()=>{sinon.assert.called(s.getContainer)});it("initializes the scene",()=>{sinon.assert.called(s.scene.load)})});describe(".remove()",()=>{let a,t;beforeEach(e=>{a=makeOne();t=a.scene;sinon.spy(LeafletLayer.layerBaseClass.prototype,"onRemove");sinon.spy(t,"destroy");let s=false;a.on("init",()=>{a.remove()});a.on("remove",()=>{if(!s){s=true;e()}});a.addTo(map)});afterEach(()=>{LeafletLayer.layerBaseClass.prototype.onRemove.restore()});it("calls the .super",()=>{sinon.assert.called(LeafletLayer.layerBaseClass.prototype.onRemove)});it("destroys the scene",()=>{sinon.assert.called(t.destroy);assert.isNull(a.scene)})});describe.skip("removing and then re-adding to a map",()=>{let a,t;beforeEach(e=>{var s=0;a=makeOne();t=a.scene;sinon.spy(a.scene,"destroy");a.on("init",()=>{s+=1;if(s===2){e()}});a.addTo(map);a.remove();a.addTo(map)});it("destroys the initial scene",()=>{sinon.assert.called(t.destroy)});it("re-initializes a new scene",()=>{assert.isTrue(a.scene.initialized)})})});