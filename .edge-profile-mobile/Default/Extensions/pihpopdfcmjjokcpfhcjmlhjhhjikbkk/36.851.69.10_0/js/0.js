moduleFor("inject",class extends AbstractTestCase{["@test injected properties should be descriptors"](e){e.ok(isClassicDecorator(inject("type")))}["@test injected properties should be overridable"](e){let t={};defineProperty(t,"foo",inject("type"));set(t,"foo","bar");e.equal(get(t,"foo"),"bar","should return the overridden value")}["@test getting on an object without an owner or container should fail assertion"](){let e={};defineProperty(e,"foo",inject("type","name"));expectAssertion(function(){get(e,"foo")},/Attempting to lookup an injected property on an object without a container, ensure that the object was instantiated via a container./)}["@test getting on an object without an owner but with a container should not fail"](t){let e={container:{lookup(e){t.ok(true,"should call container.lookup");return e}}};defineProperty(e,"foo",inject("type","name"));t.equal(get(e,"foo"),"type:name","should return the value of container.lookup")}["@test getting should return a lookup on the container"](t){t.expect(2);let e={};setOwner(e,{lookup(e){t.ok(true,"should call container.lookup");return e}});defineProperty(e,"foo",inject("type","name"));t.equal(get(e,"foo"),"type:name","should return the value of container.lookup")}["@test omitting the lookup name should default to the property name"](e){let t={};setOwner(t,{lookup(e){return e}});defineProperty(t,"foo",inject("type"));e.equal(get(t,"foo"),"type:foo","should lookup the type using the property name")}});