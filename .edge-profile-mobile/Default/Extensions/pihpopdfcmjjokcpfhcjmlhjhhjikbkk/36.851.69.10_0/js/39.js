class LastIndexOfTests extends AbstractTestCase{"@test should return index of object's last occurrence"(){let t=newFixture(3);let s=this.newObject(t);let n=3;for(let e=0;e<n;e++){this.assert.equal(s.lastIndexOf(t[e]),e,`obj.lastIndexOf(${t[e]}) should match idx`)}}"@test should return index of object's last occurrence even startAt search location is equal to length"(){let t=newFixture(3);let s=this.newObject(t);let n=3;for(let e=0;e<n;e++){this.assert.equal(s.lastIndexOf(t[e],n),e,`obj.lastIndexOfs(${t[e]}) should match idx`)}}"@test should return index of object's last occurrence even startAt search location is greater than length"(){let t=newFixture(3);let s=this.newObject(t);let n=3;for(let e=0;e<n;e++){this.assert.equal(s.lastIndexOf(t[e],n+1),e,`obj.lastIndexOf(${t[e]}) should match idx`)}}"@test should return -1 when no match is found"(){let e=this.newObject(newFixture(3));let t={};this.assert.equal(e.lastIndexOf(t),-1,"obj.lastIndexOf(foo) should be -1")}"@test should return -1 when no match is found even startAt search location is equal to length"(){let e=this.newObject(newFixture(3));let t={};this.assert.equal(e.lastIndexOf(t,get(e,"length")),-1,"obj.lastIndexOf(foo) should be -1")}"@test should return -1 when no match is found even startAt search location is greater than length"(){let e=this.newObject(newFixture(3));let t={};this.assert.equal(e.lastIndexOf(t,get(e,"length")+1),-1,"obj.lastIndexOf(foo) should be -1")}}runArrayTests("lastIndexOf",LastIndexOfTests);